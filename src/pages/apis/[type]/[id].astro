---
export const prerender = false;

const { id } = Astro.params as { type: string; id: string };

return Astro.redirect(
	`https://apis.acdh.oeaw.ac.at/apis/api2/entity/${id}/?format=rdf%2Bturtle`,
	302,
);
---
